package tools;

import umamusume.*;

import java.util.HashMap;

public class Utilities {
    public static HashMap<String, BaseUma> readBaseUmas() {
        HashMap<String, BaseUma> umas = new HashMap<>();
        // Reads "base umamusumes" and stores them in a hashmap.
        return umas;
    }

    public static HashMap<String, Uma> readUmas() {
        HashMap<String, Uma> umas = new HashMap<>();
        // Reads "umas" and stores them in a hashmap.
        return umas;
    }
}
